module Formats
  class FileGenerator
    FILE_TYPE = "format_name(override)"
    
    def self.write_to_file(data)
      warning = "You need to overwrite this method"
      return warning
    end
  end
end
